function fullName(firstName: string, lastName: string) {
    return firstName + " " + lastName;
  }

  let resultFunctionOne = fullName("dana", "almestadi");
  console.log(resultFunctionOne)

function addtion(num1: number, num2: number): number {
    return num1 + num2;
  }
  let resultFunctiontwo = addtion(16, 15);
console.log(resultFunctiontwo)